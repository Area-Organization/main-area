{
  "name": "@area/shared",
  "scripts": {
    "db:generate": "bun --cwd packages/shared prisma generate",
    "db:migrate": "bun --cwd packages/shared prisma migrate dev",
    "db:studio": "bun --cwd packages/shared prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}